# 麥麥錯題網站維護與自動收錄規範 (Wrong Question Workflow)

## 使用者核心要求
**只要使用者在對話中上傳題目圖片或文字題目，助理必須主動且自動將其收錄至錯題網站中，並完成正確分類、排版與深度解析。**

**【零奇怪符號、永久純淨繁體中文排版最高準則】**：
- **永遠以最純淨清晰的繁體中文呈現**：網站上的題幹、選項、解析、盲點提示，一律必須是自然、優美、無任何奇怪符號與未渲染程式碼（如 `\overline{...}`、`\frac{...}`、`\triangle`、`\times`、`$`）的純淨排版。
- **永久內建，絕不設開關**：這不是一個可開啟或關閉的額外功能，而是系統永遠必須遵守的內建標準。絕不可在前端頁面新增任何「清楚模式切換按鈕」或開關。
- **徹底防亂碼機制**：
  1. 選項切割與題幹解析一律使用標準行首定位，嚴禁誤將數學式中的字母（如線段 AB 中的 B）誤認為選項。
  2. 資料庫與前端渲染管線一律進行零死角淨化，將所有幾何、數學符號以最乾淨的標準數學符號（如線段頂部橫線、△、∠、×、÷）呈現。

---

### 自動收錄與分類標準作業流程 (SOP)

1. **圖文處理原則（嚴格依循）**：
   - **純文字題目（截圖內無圖表/示意圖）**：
     - **不放截圖**，`diagramUrl: ""` 設為空字串。
     - 精確提取文字，重新排版為題幹、清晰的個別選項（○ (A) ... ○ (B) ...）、標準答案、詳解與易錯盲點筆記。
   - **有圖表/示意圖題目**：
     - 若題目含有幾何圖、函數圖形、生物解剖示意圖、地圖、統計圖表等實質圖表，將其保存至 `assets/questions/`，設定 `diagramUrl: "assets/questions/..."`，供使用者點開燈箱檢視。
   - **圖表去痕與輔助線清理**：
     - 若截圖中含有作答筆跡、答案勾選標記，或解答解析所繪製的輔助線（如粉紅/紅色虛線、提示點代號），必須主動清理還原為題目的原始乾淨圖形，避免直接劇透解題思路。
   - **選項為圖片形式之題目**：
     - 完整保留圖片，並在題幹或選項說明中明確標示「哪一張圖片對應哪一個選項」，確保視覺與選項對照清晰。

2. **題目解析與學科分類 (`subject`)**：
   - `自然/生物`：細胞、遺傳、生殖、呼吸排泄、個體組成層次等。
   - `自然/理化`：力學、光學、運動學、熱學、化學反應等。
   - `數學`：乘法公式、比例、幾何、函數、不等式、級數等。
   - `國文`：字音字形、成語、文言文閱讀、應用文等。
   - `社會`：歷史、地理、公民與社會。
   - **段考範圍 (`examPeriod`)**：預設為 `一段`（或依使用者指示）。
   - **錯誤原因 (`errorReason`)**：如「觀念不懂」、「審題不清」、「計算粗心」等。
   - **核心觀念 (`concept`)**：精準標註該題考點。
   - **易錯警示筆記 (`mistakeNote`)**：以螢光黃提示常見陷阱與盲點對比。

3. **資料庫同步更新 (`js/data.js`)**：
   - 將新題目追加至 `INITIAL_SEED_DATA` 陣列中。
   - 升級 `STORAGE_KEY`（如 `v88` $\to$ `v89`），並在 `init()` 保留前版 fallback 相容鏈，確保使用者重新整理網頁時能即時無痛同步載入，不遺失原有作答統計與複習排程。

4. **Checkpoint 存檔與 GitHub 雲端網站即時同步規範**：
   - 每次完成題目收錄、圖片管理或功能微調後，主動執行 Git commit 建立專案進度存檔點，保留規範清晰的 commit message（例如：`feat: add meiosis chromosome question (A) to 2026-09-14 review list`）。
   - **必須同步執行 `git push origin main`**：將所有更新即刻推送至 GitHub 倉庫，確保 GitHub Pages 線上網站（https://b0987498500-ops.github.io/Miley-wrong-book/）永遠與本機題庫保持 100% 同步！
   - 同步更新專案根目錄的 `Checkpoints.md`，詳實紀錄版本進度。

5. **對話回覆要求**：
   - 回覆題目正解、核心破題思路與易錯觀念對照。
   - 向使用者確認題目已成功收錄至網站對應分類，並顯示分類細項（學科、觀念標籤、題號、圖表處理狀態）。

6. **複習流程與「已複習／未複習」動態隊列規範**：
   - **狀態判定**：只要使用者在題目上點擊「已擊敗」或「未擊敗」，該題即標記為【已複習】；未作答或跳過的題目維持【未複習】。
   - **卡片頂部標示**：題目上方清晰標示【已複習】（綠色打勾徽章）或【未複習】（琥珀色時鐘徽章）。
   - **動態隊列前移與後排**：
     - 作答後的題目（已複習）自動歸入隊列前半部；
     - 點擊「先跳過，晚點做」的複雜題目維持未複習，並向後排一個，讓使用者先攻克下一題；
   - **火車進度連動**：學習號列車的軌道進度條與各站點，即時反映當前「已複習題數」與完成比例。

